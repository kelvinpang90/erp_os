"""External system adapters.

Each integration target (MyInvois, etc.) defines a Protocol interface here
and provides one or more implementations. The factory module selects the
active implementation based on settings.
"""
